<template>
    <section class="w-full bg-blue text-white pt-[min(141px,19.58vw)] pb-[min(256px,35.5vw)] lg:py-44 flex flex-col px-0">
        <div class="flex flex-col mx-auto w-full" style="max-width:1920px">
            <div class="w-full max-w-[1480px] mx-auto px-[min(14%,104px)] xl:px-0">
                <h2 class="font-Calibri font-bold tracking-widest text-[min(52px,7.2vw)] xl:text-[58px] mb-[min(66px,9.16vw)] lg:mb-28">
                    {{ title.toUpperCase() }}
                </h2>
            </div>

            <!-- Desktop view - Grid layout -->
            <div class=" lg:grid grid-cols-3 gap-12 ">
                <div
                    v-for="team in teamImages"
                    :key="team.id"
                    class="min-w-[300px] flex-shrink-0 mb-3 lg:mb-0"
                >
                    <img
                        :src="team.image"
                        :alt="team.id"
                        class="w-full h-auto object-cover"
                        width="300"
                        height="200"
                        loading="lazy"
                    />
                </div>
            </div>
        </div>
    </section>
</template>

<script>
export default {
    props: {
        title: {
            type: String,
            default: 'MEET THE',
        },
        teamImages: {
            type: Object,
            required: true,

        },
    },

    name: "AboutTeamSection",
    mounted() {
        console.log(this.teamImages);
    }
};
</script>

<style scoped>
/* Hide scrollbars */
.noScrollbars::-webkit-scrollbar {
    display: none;
}
.noScrollbars {
    -ms-overflow-style: none;
    scrollbar-width: none;
}
</style>
